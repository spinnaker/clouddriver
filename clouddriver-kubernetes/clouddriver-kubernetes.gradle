dependencies {
  spinnaker.group('kubernetes')
  compile project(":clouddriver-core")
  compile project(":clouddriver-docker")
  compile spinnaker.dependency('frigga')
  compile spinnaker.dependency('bootActuator')
  compile spinnaker.dependency('bootWeb')

  //FIXME: Remove the following line when k8s client java api jar is available in maven repo
  compile fileTree(dir: 'kubernetes-client-java-libes', include: '*.jar')
}
